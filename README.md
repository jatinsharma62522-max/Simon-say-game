# Simon Says Gmae

- Game starts on key press
- A random color is added to the sequence each level
- Game sequence is stored in `gameSeq`
- User inputs are stored in `userSeq`
- Inputs are compared step-by-step
- Correct sequence increases level
- Wrong input ends the game and resets state